import React from 'react';

const Header = () => {
  return (
    <header style={{ background: '#0077cc', color: 'white', padding: '1rem' }}>
      <h1>Degree Tracker System</h1>
    </header>
  );
};

export default Header;